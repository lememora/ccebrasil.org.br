<h1>Usuários - Midiateca :: Edição</h1>

<% form_for(@medialibrary_user) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => "form", :locals => { :f => f } %>

  <p>
    <%= f.submit 'Atualizar', :disable_with => "Atualizando.." %>
  </p>
<% end %>

<%= link_to 'Exibir', @medialibrary_user %> |
<%= link_to 'Voltar', medialibrary_users_path %>